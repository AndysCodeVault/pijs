name = "setErrorMode"
description = "Sets how PI-JS handles errors by setting an error mode between throw, log, and none."
isScreen = false
parameters = [ "mode" ]
pdata = [ "Set to throw, log, or none for the different error modes." ]
example = """
$.setErrorMode( "throw" );
$.screen( "23514235" );
"""
